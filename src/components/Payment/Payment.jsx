import React, {useSelector} from 'react'

const Payment = () => {
    // const { reserveItems } = useSelector(state => state.reserve)
  return (

    <section>
        <h1>hola</h1>
    </section>
  )
}

export default Payment